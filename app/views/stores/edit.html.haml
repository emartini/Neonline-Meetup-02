.page-header
  %h2
    Edit Store

= simple_form_for @store, :url => store_path(@store), :html => { :class => "edit_store form-horizontal", :id => "edit_store" } do |f|
  %input{:name => "_method", :type => "hidden", :value =>"put"}
  = render :partial => "form", :locals => {:f => f}

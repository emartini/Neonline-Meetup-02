= render :partial => "shared/flash_alerts"
%fieldset

  = f.input :name, :input_html=>{:class => 'text_field span5'}

  = f.input :legal_name, :input_html=>{:class => 'text_field span5'}

  = f.input :rut, :input_html=>{:class => 'text_field span5'}, :label => 'RUT', :hint => "Incluya el dígito verificador"

  = f.input :description, :input_html=>{:class => 'text_area span5'}

.form-actions
  = f.button :submit, :class => 'btn btn-primary'
  or
  = link_to "Cancel", stores_path
